server {
    listen ${PORT};
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Enable gzip compression for faster load
    gzip on;
    gzip_types text/css application/javascript image/svg+xml application/json;

    # Cache static assets for 7 days
    location ~* \.(css|js|png|jpg|jpeg|svg|woff|woff2|ttf|ico)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    # SPA fallback — serves index.html for all routes
    location / {
        try_files $uri $uri/ /index.html;
    }

    # CORS — allows the Railway website domain to POST to super-agent API
    add_header Access-Control-Allow-Origin "*" always;
    add_header Access-Control-Allow-Methods "GET, POST, OPTIONS" always;
    add_header Access-Control-Allow-Headers "Content-Type" always;
}
