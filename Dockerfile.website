FROM nginx:alpine

# Copy Bridge commercial website static files
COPY website/ /usr/share/nginx/html/

# Copy nginx config — nginx listens on 8080, set PORT=8080 in Railway Variables
COPY nginx-website.conf /etc/nginx/conf.d/default.conf

EXPOSE 8080

CMD ["nginx", "-g", "daemon off;"]
